<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="#">Pet Sitter</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="d-flex justify-content-between w-100">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('dog_list') }}">Dogs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('owner_list') }}">Owners</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('bookings') }}">Bookings</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('profits') }}">Profits</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                        {% else %}
                            <div class="d-flex">
                                <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                            </div>
                        {% endif %}
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
